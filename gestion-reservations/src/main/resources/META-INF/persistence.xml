<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             version="3.0"
             xsi:schemaLocation="
               https://jakarta.ee/xml/ns/persistence
               https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <!-- Unité de persistance -->
    <persistence-unit name="reservationsPU" transaction-type="RESOURCE_LOCAL">

        <!-- ===== ENTITÉS ===== -->
        <class>com.example.model.Utilisateur</class>
        <class>com.example.model.Salle</class>
        <class>com.example.model.Reservation</class>
        <class>com.example.model.Equipement</class>

        <!-- ===== PROPRIÉTÉS ===== -->
        <properties>

            <!-- Connexion H2 -->
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:mem:reservations-db"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

        </properties>

    </persistence-unit>

</persistence>